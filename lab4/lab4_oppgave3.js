console.log("Oppgave 3 a");
var currentNumber = 1;
var lastNumber = 0;

function myFunction() {
  lastNumber = currentNumber;
  currentNumber += 2;
  return "last number: " + lastNumber + " current number: " + currentNumber;
}

console.log("Oppgave 3 b");

var tall = 0;

function helTall() {
  lastNumber = tall;
  currentNumber += 1;
  return "last number: " + lastNumber + " current number: " + currentNumber;
}

console.log("Oppgave 3 c");

function funksjon() {
  var step = 0;
  var counter =
    var limit
}